<Window x:Class="Webcal.Windows.ExceptionWindow.ExceptionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Webcal.Windows.ExceptionWindow"
        Title="Technical Information"
        WindowStartupLocation="CenterScreen"
        Icon="../../Main Icon.ico"
        Height="600"
        Width="800">
    <Window.DataContext>
        <local:ExceptionWindowViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ListView ItemsSource="{Binding Exceptions}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Date/Time"
                                    Width="100"
                                    DisplayMemberBinding="{Binding Occurred, StringFormat={}{0:dd-MMM-yyyy HH:mm}}" />
                    <GridViewColumn Header="Exception Details"
                                    Width="650"
                                    DisplayMemberBinding="{Binding ExceptionDetails}" />
                </GridView>
            </ListView.View>
        </ListView>

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center">
            <Button Content="Export"
                    Command="{Binding ExportCommand}"/>
            <Button Content="Close"
                    Command="{Binding CloseCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType={x:Type Window}}}"/>
        </StackPanel>
        
    </Grid>
</Window>
