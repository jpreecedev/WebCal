<core:BaseSettingsView x:Class="Webcal.Views.Settings.MailSettingsView"
                       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                       xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                       xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                       xmlns:vm="clr-namespace:Webcal.Views.Settings"
                       xmlns:core="clr-namespace:Webcal.Core"
                       mc:Ignorable="d"
                       d:DesignWidth="628"
                       d:DesignHeight="374">
    <UserControl.DataContext>
        <vm:MailSettingsViewModel />
    </UserControl.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="25" />
            <RowDefinition />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>

        <TextBlock Text="Email settings"
                   Style="{StaticResource SettingsTitleFont}" />

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="25" />
                <RowDefinition Height="10" />
                <RowDefinition Height="20" />
                <RowDefinition Height="30" />
                <RowDefinition Height="10" />
                <RowDefinition Height="20" />
                <RowDefinition />
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal">

                <RadioButton Content="Don't send emails"
                             IsChecked="{Binding MailSettings.DontSendEmails, Mode=OneWay}"/>

                <RadioButton x:Name="EmailCertificatesCheckBox"
                          Content="Automatically email certificates"
                          IsChecked="{Binding MailSettings.AutoEmailCertificates, UpdateSourceTrigger=PropertyChanged}"
                          Margin="30,0,0,0" />

                <RadioButton x:Name="PersonaliseEmailsCheckBox"
                          Content="Allow me to make edits before sending"
                          IsChecked="{Binding MailSettings.PersonaliseMyEmails, UpdateSourceTrigger=PropertyChanged}"
                          Margin="30,0,0,0" />

            </StackPanel>

            <TextBlock Text="Subject" 
                       IsEnabled="{Binding MailSettings.AllowEditingOfEmail, UpdateSourceTrigger=PropertyChanged}"
                       Grid.Row="2" />

            <TextBox Text="{Binding MailSettings.Subject}"
                     IsEnabled="{Binding MailSettings.AllowEditingOfEmail, UpdateSourceTrigger=PropertyChanged}"
                     Grid.Row="3"/>

            <TextBlock Text="Body"
                       IsEnabled="{Binding MailSettings.AllowEditingOfEmail, UpdateSourceTrigger=PropertyChanged}"
                       Grid.Row="5"/>

            <TextBox Text="{Binding MailSettings.Body}"
                     IsEnabled="{Binding MailSettings.AllowEditingOfEmail, UpdateSourceTrigger=PropertyChanged}"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     HorizontalAlignment="Stretch"
                     HorizontalContentAlignment="Stretch"
                     VerticalAlignment="Stretch"
                     VerticalContentAlignment="Stretch"
                     TextWrapping="Wrap"
                     Height="Auto"
                     Grid.Row="6"/>
        </Grid>
        
        <StackPanel Grid.Row="2"
                    Orientation="Vertical"
                    Margin="0,5,0,0">
            <TextBlock Text="Default Sending Preferences"/>
            <StackPanel Orientation="Horizontal"
                        Margin="0,10,0,0">
                <CheckBox Content="Send To Office"
                          IsChecked="{Binding WorkshopSettings.SendToOffice}"
                          Margin="0,0,10,0" />
                <CheckBox Content="Send To Customer"
                          IsChecked="{Binding WorkshopSettings.SendToCustomer}"
                          Margin="0,0,10,0" />
                <CheckBox Content="Do not send"
                          IsChecked="{Binding WorkshopSettings.DoNotSend}" />
            </StackPanel>
        </StackPanel>
        
    </Grid>
</core:BaseSettingsView>
